apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hello
spec:
  values:
    hosts:
    - hello.production.euw
    - hello.data.svc.cluster.local
    image:
      tag: 1.0.10 # {"$imagepolicy": "data:production-euw-hello:tag"}
      automation:
        semver:
          expr: 1.x.x
