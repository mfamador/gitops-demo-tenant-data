apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: critical
spec:
  values:
    hosts:
      - critical.staging.eun
      - critical.data.svc.cluster.local
    image:
      tag: 0.9.1 # {"$imagepolicy": "data:staging-eun-critical:tag"}
