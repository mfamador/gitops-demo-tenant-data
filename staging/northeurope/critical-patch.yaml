apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: critical
spec:
  values:
    hosts:
    - critical.staging.eun
    - critical.data.svc.cluster.local
    image:
      tag: 1.0.0 # {"$imagepolicy": "data:staging-eun-critical:tag"}
      automation:
        semver:
          expr: 1.x.x
